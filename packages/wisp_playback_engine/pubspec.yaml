name: wisp_playback_engine
description: "A simple, dual-player, crossfade-capable audio playback engine."
version: 26.9.1+1

resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  media_kit: ^1.2.6

  wisp_audio_output_info: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter: