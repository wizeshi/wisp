/// Spotify lyrics provider is disabled for now.
library;

import '../../models/metadata_models.dart';

class SpotifyLyricsProvider {
  Future<LyricsResult?> getLyrics(String trackId) async => null;
}
